// console.log("test: main.js");       Test. Works.

// Create a DOM element in your index.html with a class of foodList.
// Create a function which returns HTML String Template (template is the HTML representation for a food item).
// Create a function that inserts an HTML representation of a food into the DOM
    // STEP 1: Get Resources from Food API/Fetch (data.js)
    // STEP 2: Create Resource Document Fragments (domComponents.js)
    // STEP 3: Append Resources to DOM (domBuilder.js)


    //All of this code (lines 12-19) does the same thing as the code on foodapi.js lines 3-7
    // fetch("http://localhost:8088/food")              
    // .then(foods => foods.json())
    // .then(parsedFoods => {
    //     console.log(parsedFoods);
    //     foodArray = parsedFoods;
    //     console.table(foodArray);
    //     })






//let newSection = document.createElement()     //See if you can add this to a button
